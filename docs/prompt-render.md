# sdt prompt render

> Generated by `sdt` dev. Regenerate with `sdt context docs`.

## Usage

```
sdt prompt render [flags]
```

## Description

Render a prompt template with variables

Render a Go text/template prompt with JSON or YAML variables.

Template source priority:
  1. --template flag (inline string)
  2. --file flag (path to template file)
  3. stdin

Variable source:
  1. --vars flag (inline JSON or YAML)
  2. --vars-file flag (path to JSON or YAML file)

Example:
  sdt prompt render --template "You are {{.role}}." --vars '{"role":"assistant"}'
  sdt prompt render --file system.txt --vars-file context.json

## Flags

| Flag | Shorthand | Type | Default | Description |
|---|---|---|---|---|
| `file` |  | string |  | Path to template file |
| `model` |  | string | gpt-4 | Model for token counting (used with --show-tokens) |
| `show-tokens` |  | bool |  | Include token count in output |
| `template` |  | string |  | Inline template string |
| `vars` |  | string |  | Variables as inline JSON or YAML |
| `vars-file` |  | string |  | Path to JSON or YAML variables file |

## Global Flags

| Flag | Type | Default | Description |
|---|---|---|---|
| `file` | string |  | Input File |
| `format` | string | text | Output format: text\|json\|yaml |
| `inb64` | bytesBase64 |  | Input Base 64 |
| `input` | string |  | Input String |
| `no-color` | bool |  | Disable ANSI color codes |
| `quiet` | bool |  | Suppress informational messages, only output result |

## Input / Output

- Input text: stdin | `--input` | `--file` | `--inb64` (in that precedence).
- Machine-readable output: `--format json` or `--format yaml`.
- Exit code `0` on success, `1` on error (message on stderr).

