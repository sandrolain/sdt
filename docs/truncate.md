# sdt truncate

> Generated by `sdt` dev. Regenerate with `sdt context docs`.

## Usage

```
sdt truncate [flags]
```

## Description

Truncate text to a maximum number of LLM tokens

Truncate input text so that it fits within a maximum token budget.

Strategies:
  chars       Hard cut at character level (default)
  sentences   Cut at the last complete sentence boundary
  sections    Cut at the last complete markdown section boundary

Example:
  cat long_doc.md | sdt truncate --max-tokens 4000
  sdt truncate --max-tokens 2000 --strategy sentences --file essay.txt
  sdt truncate --max-tokens 1000 --strategy sections --model claude --file README.md

## Flags

| Flag | Shorthand | Type | Default | Description |
|---|---|---|---|---|
| `max-tokens` |  | int | 4096 | Maximum number of tokens to keep |
| `model` |  | string | gpt-4 | Model name for tokenizer selection |
| `strategy` |  | string | chars | Truncation strategy: chars\|sentences\|sections |

## Global Flags

| Flag | Type | Default | Description |
|---|---|---|---|
| `file` | string |  | Input File |
| `format` | string | text | Output format: text\|json\|yaml |
| `inb64` | bytesBase64 |  | Input Base 64 |
| `input` | string |  | Input String |
| `no-color` | bool |  | Disable ANSI color codes |
| `quiet` | bool |  | Suppress informational messages, only output result |

## Input / Output

- Input text: stdin | `--input` | `--file` | `--inb64` (in that precedence).
- Machine-readable output: `--format json` or `--format yaml`.
- Exit code `0` on success, `1` on error (message on stderr).

