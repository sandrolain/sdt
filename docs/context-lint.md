# sdt context lint

> Generated by `sdt` dev. Regenerate with `sdt context docs`.

## Usage

```
sdt context lint [flags]
```

## Description

Validate context frontmatter and links

Validate the context/ documents: frontmatter well-formed (kind, mandatory
summary), [[links]] resolve to existing files, and ADR filenames/numbers are
consistent. Exits non-zero when CRITICAL issues are found.

Examples:
  sdt context lint
  sdt context lint --format json

## Global Flags

| Flag | Type | Default | Description |
|---|---|---|---|
| `file` | string |  | Input File |
| `format` | string | text | Output format: text\|json\|yaml |
| `inb64` | bytesBase64 |  | Input Base 64 |
| `input` | string |  | Input String |
| `no-color` | bool |  | Disable ANSI color codes |
| `quiet` | bool |  | Suppress informational messages, only output result |

## Input / Output

- Input text: stdin | `--input` | `--file` | `--inb64` (in that precedence).
- Machine-readable output: `--format json` or `--format yaml`.
- Exit code `0` on success, `1` on error (message on stderr).

